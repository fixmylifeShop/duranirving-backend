<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <title>Document</title>

  <style>
    body {
      margin: 20px;
      width: 100%;
      font-family: Arial, Helvetica, sans-serif;
    }

    .pageContainer {
      margin: auto;
      min-width: 325px;
      width: 60%;
      padding: 10px;
      text-align: center;
      background: rgb(201, 201, 201);
    }

    .imgContainer {
      background: white;
      width: 100px;
      height: 100px;
      border-radius: 100%;
      border: solid;
      overflow: hidden;
      margin: auto;
    }

    .imgContainer img {
      width: 100%;
      height: 100%;
    }

    .infoContainer {
      background: white;
      text-align: left;
    }

    .infoTopic {
      border-bottom: 2px solid rgb(201, 201, 201);
      padding: 10px;
    }

    .infoTopic h4 {
      margin: 0;
    }

    .infoTopic p {
      margin: 0;
      font-family: "Courier New", Courier, monospace;
      font-size: 16px;
    }

    .infoTopic a {
      margin: 0;
      font-family: "Courier New", Courier, monospace;
      font-size: 16px;
    }

    .timeSubmitted {
      text-align: center;
      padding: 10px;
    }
  </style>
</head>

<body>
  <div class="pageContainer">
    <div class="imgContainer">
      <img src="https://www.duranirving.com/static/media/cartoon.72f7f183.jpg" />
    </div>
    <h2>NEW EMAIL SUBMISSION</h2>
    <h3>
      Someone just submitted a form on <a href="{{ url }}">{{ url }}</a>.
    </h3>
    <div class="infoContainer">
      <div class="infoTopic">
        <h4>Fullname:</h4>
        <p>{{ name }}</p>
      </div>
      <div class="infoTopic">
        <h4>Email:</h4>
        <a href="{{ email }}">{{ email }}</a>
      </div>
      {{#if phone}}
      <div class="infoTopic">
        <h4>Telephone:</h4>
        <a href="tel:{{ phone }}">{{ phone }}</a>
      </div>
      {{/if}}
      <div class="infoTopic">
        <h4>Message:</h4>
        <p>{{ message }}</p>
      </div>
      <div class="timeSubmitted">
        <p>Submitted at {{ time }}.</p>
      </div>
    </div>
  </div>
</body>

</html>